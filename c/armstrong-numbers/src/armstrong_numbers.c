#include "stdio.h"
#include "armstrong_numbers.h"

bool is_armstrong_number(int candidate){

    // Example 154 ( NOT )
    // 154 != 1^3 + 5^3 + 4^3
    // = 1 + 125 + 64 = 190

    // get candidate INT length ( ??? ) [ 3 ]
    // create new char[] with length [ 3 - 1 ] ( 0 based counting )
    // set

    printf("%d\n", candidate);
    return false;
}
