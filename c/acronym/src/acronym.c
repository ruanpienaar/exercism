#include "acronym.h"
#include <stdio.h>
#include <string.h>

char * abbreviate(const char *phrase){
    // char *response = "";
    //printf("SIZE(%s) = %lu \n", phrase, sizeof(*phrase));
    if(phrase == NULL){ // If NULL
        return NULL;
    } else {
        return "PNG";
    }
}
